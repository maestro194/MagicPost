import { createSlice } from "@reduxjs/toolkit";

const initialState = {

};

const weSlice = createSlice({
    name: 'we',
    initialState,
    reducers: {

    }
});

export const { } = weSlice.actions;

export default weSlice.reducer;